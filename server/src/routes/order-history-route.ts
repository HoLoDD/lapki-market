import Router, { Application } from 'express';
const router: Application = Router();

router.post('/');
router.get('/');
router.get('/:id');
router.put('/');
router.delete('/');

export default router;
